# The default stack (PROVIDER=mock) needs only python-dotenv — everything else
# is the Python standard library. That's deliberate: the whole production stack
# runs offline with no key and almost no dependencies.

# Loads variables from a .env file (like PROVIDER) into the environment.
python-dotenv>=1.0.0

# --- Provider SDKs (OPTIONAL) ---
# You only need these if you switch PROVIDER away from the offline mock to point
# the same pipeline at a real model. Install the one you want, or both.

# OpenAI — used when PROVIDER=openai.
openai>=1.30.0

# Anthropic — used when PROVIDER=claude.
anthropic>=0.111.0
